name: Test and deploy - MacOS
on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: macos-latest
    name: Deployment on MacOS
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-python@v1
        with:
          python-version: "3.8"
          architecture: "x64"
      - uses: actions/setup-java@v1
        with:
          java-version: "11.0.2"
          java-package: jdk # (jre, jdk, or jdk+fx) - defaults to jdk
          architecture: x64
      - name: Java check
        run: java --version 

      - name: Install pipenv Linux
        run: pip3 install pipenv

      - name: Build JDK
        run: jlink --no-header-files --no-man-pages --compress=2 --strip-debug --add-modules java.se,jdk.unsupported --output jdk4py/java-runtime

      - name: Install python dependencies
        run: pipenv install --dev --python 3.8

      - name: Test
        run: pipenv run pytest

      - name: Build MacOS wheel
        run: pipenv run python setup.py bdist_wheel --plat-name macosx_10_9_x86_64

      - name: Deploy to Pypi
        env:
          TWINE_PASSWORD: ${{ secrets.TWINE_PASSWORD }}
        run: pipenv run twine upload dist/* --username __token__ --password $TWINE_PASSWORD
