[tool.poetry]
name = "jdk4py"
# Poetry requires this but we do not use it.
version = "0.1.0"
description = ""
authors = []

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]
black = "*"
mypy = "*"
pytest = "*"
setuptools = "*"
twine = "*"
wheel = "*"

[tool.mypy]
strict = true
