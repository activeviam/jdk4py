[tool.poetry]
name = "jdk4py"
# Poetry requires this but we do not use it.
version = "0.1.0"
description = ""
authors = []

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]
black = "*"
pytest = "*"
setuptools = "*"
twine = "*"
wheel = "*"
