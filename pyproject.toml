[tool.poetry]
name = "jdk4py"
# Poetry requires this but we do not use it.
version = "0.1.0"
description = ""
authors = []

[tool.poetry.dependencies]
python = "^3.6"

[tool.poetry.dev-dependencies]
setuptools = "*"
twine = "*"
wheel = "*"
pytest = "*"
